{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build BrainFudger",
      "type": "process",
      "command": "dotnet",
      "args": [
        "build",
        "${workspaceFolder}/BrainFudger/BrainFudger.csproj",
        "-p:PublishAot=false",
        "-nologo"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": "$msCompile",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
